import { TestBed } from '@angular/core/testing'; import { HttpTestingController,provideHttpClientTesting } from '@angular/common/http/testing'; import { provideHttpClient } from '@angular/common/http'; import { TicketService } from './ticket.service';
describe('TicketService',()=>{let service:TicketService;let http:HttpTestingController;beforeEach(()=>{TestBed.configureTestingModule({providers:[provideHttpClient(),provideHttpClientTesting()]});service=TestBed.inject(TicketService);http=TestBed.inject(HttpTestingController)});afterEach(()=>http.verify());it('sends pagination and server filters',()=>{service.list({search:'login',status:'New',overdueOnly:true,page:2,pageSize:10}).subscribe();const r=http.expectOne(x=>x.url.endsWith('/api/tickets'));expect(r.request.params.get('search')).toBe('login');expect(r.request.params.get('status')).toBe('New');expect(r.request.params.get('overdueOnly')).toBe('true');expect(r.request.params.get('page')).toBe('2');r.flush({items:[],page:2,pageSize:10,totalCount:0,totalPages:0})});it('uses the dedicated status endpoint',()=>{service.changeStatus(42,'Resolved').subscribe();const r=http.expectOne('http://localhost:5000/api/tickets/42/status');expect(r.request.method).toBe('PUT');expect(r.request.body).toEqual({status:'Resolved'});r.flush({})})});
