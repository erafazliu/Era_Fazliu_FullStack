import { Component,OnInit,inject } from '@angular/core'; import { CommonModule } from '@angular/common'; import { FormBuilder,ReactiveFormsModule,Validators } from '@angular/forms'; import { ActivatedRoute,Router,RouterLink } from '@angular/router'; import { Priority } from './models'; import { TicketService } from './ticket.service';
@Component({standalone:true,imports:[CommonModule,ReactiveFormsModule,RouterLink],template:`<a [routerLink]="editing?['/tickets',id]:['/']" class="back">← {{editing?'Back to ticket':'Back to tickets'}}</a><section class="form-shell"><div class="page-head"><div><p class="eyebrow">{{editing?'Ticket maintenance':'New request'}}</p><h1>{{editing?'Edit ticket':'Create a ticket'}}</h1><p>{{editing?'Update the customer and issue details.':'Capture the issue so the support team can take action.'}}</p></div></div><form class="panel" [formGroup]="form" (ngSubmit)="save()"><div class="field-row"><label>Title<input formControlName="title" placeholder="Brief summary of the issue"><small *ngIf="invalid('title')">A title is required (maximum 160 characters).</small></label><label>Priority<select formControlName="priority"><option>Low</option><option>Normal</option><option>High</option><option>Critical</option></select></label></div><label>Description<textarea formControlName="description" rows="7" placeholder="What happened, what was expected, and any useful context"></textarea><small *ngIf="invalid('description')">A description is required.</small></label><div class="section-title"><h2>Customer information</h2><p>Who should we contact about this request?</p></div><div class="field-row"><label>Full name<input formControlName="customerName" placeholder="Customer name"><small *ngIf="invalid('customerName')">Customer name is required.</small></label><label>Email address<input type="email" formControlName="customerEmail" placeholder="customer@company.com"><small *ngIf="invalid('customerEmail')">Enter a valid email address.</small></label></div><p class="error" *ngIf="error">{{error}}</p><div class="form-actions"><a [routerLink]="editing?['/tickets',id]:['/']" class="button secondary">Cancel</a><button class="button" [disabled]="saving">{{saving?'Saving…':editing?'Save changes':'Create ticket'}}</button></div></form></section>`,styles:[`.form-shell{max-width:900px;margin:auto}.form-shell form{padding:36px;display:grid;gap:22px}.section-title{border-top:1px solid var(--line);padding-top:24px;margin-top:4px}.section-title h2,.section-title p{margin:0}.section-title p{color:var(--muted);margin-top:4px}.form-actions{display:flex;justify-content:flex-end;gap:12px;border-top:1px solid var(--line);padding-top:24px}label{display:grid;gap:8px}label small{color:var(--red)}@media(max-width:650px){.form-shell form{padding:22px}}`]})
export class TicketFormComponent implements OnInit {private fb=inject(FormBuilder);private api=inject(TicketService);private route=inject(ActivatedRoute);private router=inject(Router);id=0;editing=false;saving=false;error='';form=this.fb.nonNullable.group({title:['',[Validators.required,Validators.maxLength(160)]],description:['',[Validators.required,Validators.maxLength(4000)]],customerName:['',[Validators.required,Validators.maxLength(120)]],customerEmail:['',[Validators.required,Validators.email,Validators.maxLength(254)]],priority:this.fb.nonNullable.control<Priority>('Normal',Validators.required)});ngOnInit(){this.id=+this.route.snapshot.params['id'];this.editing=!!this.id;if(this.editing)this.api.get(this.id).subscribe(t=>this.form.patchValue({title:t.title,description:t.description,customerName:t.customerName,customerEmail:t.customerEmail,priority:t.priority}))}invalid(name:string){const c=this.form.get(name);return !!c&&c.invalid&&(c.dirty||c.touched)}save(){this.form.markAllAsTouched();if(this.form.invalid)return;this.saving=true;const op=this.editing?this.api.update(this.id,this.form.getRawValue()):this.api.create(this.form.getRawValue());op.subscribe({next:t=>this.router.navigate(['/tickets',t.id]),error:e=>{this.error=e.error?.detail||'The ticket could not be saved.';this.saving=false}})}}
